[package]
name = "theory_grabber"
version = "0.9.1"
authors = ["Narfinger <Narfinger@users.noreply.github.com>"]
edition = "2018"


[dependencies]
app_dirs = "1.2"         # Put your app's data in the right place on every platform
clap = "2.32"               # A simple to use, efficient, and full featured  Command Line Argument Parser 
chrono = {version = "0.4", features = ["serde"]}              # Date and time library for Rust
chrono-tz = "0.5"         # TimeZone implementations for rust-chrono from the IANA database
cursive = "0.8"            # A TUI (Text User Interface) library focused on ease-of-use.
cursive_table_view = "0.5" # A basic table view implementation for cursive.
error-chain = "0.11"           # Yet another error boilerplate library.
indicatif = "0.9"    # A progress bar and cli reporting library for Rust
rayon = "1.0"               # Simple work-stealing parallelism for Rust
reqwest = "0.9"             # higher level HTTP client library
#reqwest = {git = "https://github.com/seanmonstar/reqwest" }
select = "0.4.2"                         # A library to extract useful data from HTML documents, suitable for web scraping.
serde = "1.0"                      # A generic serialization/deserialization framework
serde_json = "1.0"
serde_derive = "1.0"               # Macros 1.1 implementation of #[derive(Serialize, Deserialize)]
serde_yaml = "0.8"               # YAML support for Serde
tempdir = "0.3"           # A library for managing a temporary directory and deleting all contents when it's dropped.
tokio-core = "0.1"             # Core I/O and event loop primitives for asynchronous I/O in Rust. Foundation for therest of the tokio crates. 
toml = "0.4"                   # A native Rust encoder and decoder of TOML-formatted files and streams. Provides implementations of the standard Serialize/Deser…
oauth2 = "1.3"                        # Bindings for exchanging OAuth 2 tokens
quick-xml = "0.12"               # High performance xml reader and writer
nom = "4.0"                 # A byte-oriented, zero-copy, parser combinators library
url = "1.7"                  # URL library for Rust, based on the WHATWG URL Standard
